/*package akka.testkit

import java.util.concurrent.TimeUnit

abstract class BlockingDeque[T] extends java.util.ArrayDeque[T]

class LinkedBlockingDeque[T] extends BlockingDeque[T] {

  def pollFirst(timeout: Long, unit: TimeUnit): T = pollFirst()

  def takeFirst(): T = pollFirst()
}
*/
